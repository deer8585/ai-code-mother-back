spring:
  application:
    name: ai-code-mother-name
server:
  port: 8080
  servlet:
    context-path: /api

# springdoc-openapi????
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.wyb.aicodemotherback.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn